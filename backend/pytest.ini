[pytest]
# deepeval registers a global pytest11 plugin that hard-fails on import
# (missing optional dep "rich") in this environment. It has nothing to do
# with this project; disable it so `pytest` works out of the box here.
addopts = -p no:deepeval
